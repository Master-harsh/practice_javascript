// Use a for loop to iterate over the array and log each element to the console

// const Array = [10, 20, 30, 40, 50, 60, 70, 80]
// for (let index = 0; index < Array.length; index++) {
//     const element = Array[index];
//     console.log(element);
    
// }

// Use foreach method to iterate over the array and log each element to the console 
const Array = [10, 20, 30, 40, 50, 60, 70, 80]
const foreachFunc = Array.forEach(element => {
    console.log(element);
    
});
