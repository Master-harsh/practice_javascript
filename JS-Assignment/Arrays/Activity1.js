// Create an array of numbers from 1 to 5 and log the array to console

const arr = [1, 2, 3, 4, 5]
console.log(arr);

// Access the first and last element of the array and log them in console

const first = arr[0];
const last = arr[4]

console.log(first);
console.log(last);

