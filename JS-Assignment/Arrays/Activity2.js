// use the push method to add a new number to the end of the array and log the updated array

const arr = [1, 2, 3, 4, 5]
// console.log(arr);

// arr.push(6)

// console.log(arr);

// use the pop method to remove the last element from the array and log the result in console

// arr.pop(5)
// console.log(arr);

// use the shift the method to remove the the first element from array and log the updated array

// arr.shift()
// console.log(arr);

// use the unshift method to add element to the begining of the array

arr.unshift(9)
console.log(arr);

