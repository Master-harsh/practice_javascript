// write a function expression to find the maximum of  two numbers and log the result to console
// const findmax = function(num1, num2){
//     return (num1 > num2) ? num1 : num2;
// }

// const number1 = 10
// const number2 = 20

// const max = findmax(number1, number2);
// console.log(`The maximum of ${number1} and ${number2} is: ${max}`);

// write a function expression to concatenate two strings and return the result

const ToConcatenateString = function(str1, str2) {
    return str1 + str2;
}

const String1 = "Harsh "
const String2 = "Tiwari"

const result = ToConcatenateString(String1, String2)
console.log(result);
