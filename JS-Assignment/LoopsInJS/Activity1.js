// Task-1 : number from 1 to 10

// let n = 10
for (let index = 1; index <= 10; index++) {
    const element = index;
    // console.log(element);
    
}

// Task-2 : table of five

for (let index = 0; index < 10; index++) {
    const element = 5 * index;
    // console.log(element);
    console.log(`5 * ${index} = ${element}`);
    
    
}